值 + 运算符
运算符有：1.数字运算符    2.布尔运算符   3.引用类型访问符   4.数组访问符   5.字符串运算符  6.比较运算符
1.数字运算符
int v1 = 1 + 2;
int v2 = 1 - 2;
int v3 = 2L * 4L;
double v4 = 9.9/3.3;
int v5 = -1;

(如果两侧的不同类型，会强制转换为同一类型再去进行计算)
long v6 = 1L + 2;

  {大小排序}  int < long < flot < double
(把int类型强制转换为long类型，是把一个范围小的转换到范围大的，而且这是显性类型的转换)
long v7 = (long)1;

(这样是隐形的类型转换)
long v8 = 1;

int v9 = 5/2;     整数除法 = 求商
int v10 = 5.0f/2  这个计算就是2.5f
（如果需要两个整数的除法，得到正确的数学结果的话,至少将一个整数强制转换为浮点数）
float v11 = (float)5/2;
float v12 = 5/(float)2;

还有很多运算符都是使用函数的方式进行计算的了 

int x = 1;
int y = 2;
int max = (x>y) ? x:y;
